
&НаКлиенте
Процедура ОтчетВыбор(Элемент, Область, СтандартнаяОбработка)
	
	#Если Не ВебКлиент Тогда
		Если Найти(Область.Текст, ":\") > 0 ИЛИ Найти(Область.Текст, ":/") > 0 Тогда // Путь к файлу
			РаботаСФайламиКлиент.ОткрытьПроводникСФайлом(Область.Текст);
		КонецЕсли;
	#КонецЕсли

КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Отчет = РаботаСФайлами.ИмпортФайловСформироватьОтчет(Параметры.МассивИменФайловСОшибками);
	
	Если Параметры.Свойство("Заголовок") Тогда
		Заголовок = Параметры.Заголовок;
	КонецЕсли;	
КонецПроцедуры
