// Возвращает признак наличия в конфигурации функциональной опции ПрефиксыОрганизаций.
//
// Возвращаемое значение:
//  Булево - признак наличия в конфигурации функциональной опции с заданным именем.
//
Функция ЕстьФункциональнаяОпцияПрефиксыОрганизаций() Экспорт
	Возврат ЕстьФункциональнаяОпция("ПрефиксыОрганизаций");
КонецФункции

// Возвращает признак наличия в конфигурации функциональной опции с заданным именем.
//
// Параметры:
//  ИмяФункциональнойОпции - Строка - имя функциональной опции, наличие которой в конфигурации необходимо проверить.
// 
// Возвращаемое значение:
//  Булево - признак наличия в конфигурации функциональной опции ПрефиксИнформационнойБазы.
//
Функция ЕстьФункциональнаяОпцияПрефиксИнформационнойБазы() Экспорт
	Возврат ЕстьФункциональнаяОпция("ПрефиксИнформационнойБазы");
КонецФункции	

// Возвращает признак наличия в конфигурации функциональной опции с заданным именем.
//
// Параметры:
//  ИмяФункциональнойОпции - Строка - имя функциональной опции, наличие которой необходимо проверить.
// 
// Возвращаемое значение:
//  Булево - признак наличия в конфигурации функциональной опции с заданным именем.
//
Функция ЕстьФункциональнаяОпция(ИмяФункциональнойОпции)
	
	Возврат Метаданные.ФункциональныеОпции.Найти(ИмяФункциональнойОпции) <> Неопределено;
	
КонецФункции