&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ЕстьФайлыВТомах = Ложь;
	
	КоличествоФайлов = ФайловыеФункцииПереопределяемый.ПолучитьКоличествоФайловВТомах();
	Если КоличествоФайлов <> 0 Тогда
		ЕстьФайлыВТомах = Истина;
	КонецЕсли;
	
	Если НЕ ЕстьФайлыВТомах Тогда
		Предупреждение(НСтр("ru = 'В информационной базе отсутствуют файлы в томах.'"));
		Возврат;
	КонецЕсли;

	ОткрытьФормуМодально("ОбщаяФорма.ВыборПутиКАрхивуФайловТомов");
	
КонецПроцедуры

